# Packages the target VM's Core installs at runtime (custom components: tapo_control, openplantbook, hydropeak).
# Listed from the live container vs its image on 2026-09-25. On 3.15, HA's wheel index is cp314-only and the image
# has no compiler, so anything compiled must be built here. A pin here replaces the image's pin.
json-timeseries==0.1.7
multidict==6.9.1
openplantbook-sdk==0.6.1
pytapo==3.4.19
rtp==0.0.4
